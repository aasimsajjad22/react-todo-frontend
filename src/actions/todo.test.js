import { submitTodo } from "./todo";
import types from "./types";

describe('Actions', () => {
    const todoText = 'Todo from testcase';

    it('Should create an action to add a todo', () => {
        
    });
})